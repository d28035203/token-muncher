# Token Muncher

**Subject vibe:** Compiler Design — lexical analysis (scanner).

Tiny lexer: identifiers, numbers, operators, punctuation. No full parser.

```bash
python3 munch.py 'sum = a + 42;'
```

MIT · practice project
